<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunseen.spcontrolsystem">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.BaseActivity"/>
        <activity android:name=".activity.SelfDialogActivity" android:theme="@style/MyDialogActivityTheme"/>
        <activity android:name=".activity.SystemLogDialogActivity" android:theme="@style/MyDialogActivityTheme"/>
        <activity android:name=".activity.BigTxtReaderActivity" android:theme="@style/MyTxtActivityTheme"/>
        <activity android:name=".activity.AddUserActivity" android:theme="@style/MyDialogActivityTheme"/>
        <activity android:name=".activity.CheckActivity" android:theme="@style/MyDialogActivityTheme"/>
    </application>

</manifest>